{
  "name": "extension",
  "version": "1.5.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/content.ts --bundle --outfile=dist/content.js --platform=browser --target=es2020 && esbuild src/background.ts --bundle --outfile=dist/background.js --platform=browser --target=es2020 && esbuild popup/popup.ts --bundle --outfile=popup/popup.js --platform=browser --target=es2020",
    "watch": "npm run build -- --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/chrome": "^0.1.40",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3"
  }
}